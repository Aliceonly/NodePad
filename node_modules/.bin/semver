#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../_semver@5.7.1@semver/bin/semver" "$@"
else 
  exec node  "$basedir/../_semver@5.7.1@semver/bin/semver" "$@"
fi
